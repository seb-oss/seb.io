import type { Metadata } from "next";

export default async function Loading() {
  return (
    <main>
      <section>
        <div>
          <h1>Loading...</h1>
        </div>
      </section>
    </main>
  );
}
